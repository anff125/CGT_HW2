***YOUR PYTHON VERSION MUST >= 3.10!***

install:
python3 -m pip install --upgrade pip
pip3 install -r requirements.txt
(csie workstation: pip3 install -r requirements.txt --break-system-packages)

optional package: uvloop
pip3 install uvloop==0.21.0

exec:
python3 EWN_client.pyz
